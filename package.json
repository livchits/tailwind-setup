{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "postcss src/styles.css -o public/styles.css && cp src/*.html public/",
    "build:css": "tailwind build src/styles.css -o public/styles.css && cp src/*.html public/",
    "watch": "watch 'npm run build:css' ./src",
    "dev": "npm run watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cssnano": "^4.1.10",
    "postcss-cli": "^7.1.1",
    "tailwindcss": "^1.4.6"
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
